<?php
//Config File

//API key to use
$apiKey = '';

//Output type, can be html, json or cli (console friendly format)
$outputType = 'html';

//Include expired domains in the results
$listExpired = true;

//Number of days to consider in the expiration window (show domains expiring within n days)
$expirationWindow = 30;